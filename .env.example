# Server
PORT=3000
NODE_ENV=production
SITE_URL=https://orina.com

# Contact form recipient
CONTACT_TO_EMAIL=hello@orina.com

# SMTP (optional — if left blank, contact messages are only saved to data/messages.json)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Orina Portfolio <no-reply@orina.com>"
